# Development API proxy is pre-configured in vite.config.js to point to http://localhost:5000.
# If deploying frontend separate from the backend (e.g. static host on Vercel and server on Render),
# you can use relative paths or configure a custom API endpoint in your environment.
VITE_API_BASE_URL=/api
